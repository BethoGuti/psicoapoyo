import { Suspense } from "react";
import { Formulario } from "../ui/AgendaCita/Formulario";

export default function AgendarCita() {
  return (
    <main className="px-2 py-20 sm:p-24">
      {/* <Suspense>
        <Formulario />
      </Suspense> */}
    </main>
  );
}
